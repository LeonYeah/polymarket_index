# API Samples

This directory stores sanitized API probe output.

Generated files named `*.local.json` or `*.local.jsonl` are ignored by Git. Committed samples must not contain private keys, cookies, bearer tokens, signatures, or trading credentials.

